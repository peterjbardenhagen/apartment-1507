# Supabase (Settings > API in the Supabase dashboard, project aaubrlnsnidxqezoxsar)
VITE_SUPABASE_URL=https://aaubrlnsnidxqezoxsar.supabase.co
VITE_SUPABASE_ANON_KEY=

# Server-only — used by api/create-tenant.ts, NEVER prefix this with VITE_
# (that would ship it to the browser). Set this in Vercel's Environment
# Variables, not in a committed .env file.
SUPABASE_SERVICE_ROLE_KEY=

# Admin Email (where enquiries are sent)
VITE_ADMIN_EMAIL=peter@bardenhagen.xyz

# Application Configuration
VITE_APP_NAME=Apartment 1507
VITE_APP_ENV=development
